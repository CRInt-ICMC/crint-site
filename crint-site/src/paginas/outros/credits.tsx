import PageLoader from '../../componentes/PageLoader';

const Creditos = () => {
    return (
        <div id='credits-root'>
            <PageLoader uid='creditos' />
        </div>
    );
}

export default Creditos;