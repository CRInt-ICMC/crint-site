import PageLoader from '../../componentes/PageLoader';

const Guias = () => {
    return (
        <div id='guias-root'>
             <PageLoader uid='estrangeiros' />
        </div>
    );
}

export default Guias;